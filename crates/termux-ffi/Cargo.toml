[package]
name = "termux-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib", "staticlib"]

[dependencies]
termux-core = { path = "../termux-core" }
