[package]
name = "termux-exec"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
termux-pty = { path = "../termux-pty" }
termux-runtime = { path = "../termux-runtime" }
